<Window x:Class="DoHoangQuocPhongWPF.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DoHoangQuocPhongWPF"
        mc:Ignorable="d"
        Title="DoHoangQuocPhong - SE182291 - Report" Height="500" Width="880" WindowStartupLocation="CenterScreen"
        Background="#f0f2f5">
    <Border Background="White" CornerRadius="10" Padding="20" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,10">
                <Button x:Name="btnBack" Content="Back" Width="90" Height="30"
                        DockPanel.Dock="Left" Click="btnBack_Click" Background="#c0392b" Foreground="White" Margin="0,0,10,0"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                    <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
                    <DatePicker x:Name="dpFrom" Width="130" Margin="0,0,10,0"/>
                    <TextBlock Text="To:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
                    <DatePicker x:Name="dpTo" Width="130" Margin="0,0,10,0"/>
                    <Button x:Name="btnFilter" Content="Filter" Width="90" Height="30" Click="btnFilter_Click"
                            Background="#3498db" Foreground="White"/>
                </StackPanel>
            </DockPanel>

            <DataGrid x:Name="dgOrderReport" Grid.Row="1" AutoGenerateColumns="False" 
                      CanUserAddRows="False" SelectionMode="Single" Margin="0" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Order ID" Binding="{Binding OrderId}" Width="120"/>
                    <DataGridTextColumn Header="Customer" Binding="{Binding Customer.ContactName}" Width="200"/>
                    <DataGridTextColumn Header="Employee" Binding="{Binding Employee.Name}" Width="200"/>
                    <DataGridTextColumn Header="Order Date" Binding="{Binding OrderDate}" Width="180"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</Window>
